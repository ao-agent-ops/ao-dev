[project]
name = "ao-examples-subruns"
version = "0.1.0"
description = "AO debug examples for subruns/concurrent execution"
requires-python = ">=3.10"
dependencies = [
    "ao-dev",
    "openai",
]

[tool.uv.sources]
ao-dev = { path = "../../..", editable = true }
