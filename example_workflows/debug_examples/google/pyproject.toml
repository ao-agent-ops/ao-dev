[project]
name = "ao-examples-google"
version = "0.1.0"
description = "AO debug examples for Google/VertexAI"
requires-python = ">=3.10"
dependencies = [
    "ao-dev",
    "google-genai",
]

[tool.uv.sources]
ao-dev = { path = "../../..", editable = true }
