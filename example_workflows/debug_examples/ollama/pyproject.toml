[project]
name = "ao-examples-ollama"
version = "0.1.0"
description = "AO debug examples for Ollama"
requires-python = ">=3.10"
dependencies = [
    "ao-dev",
    "ollama",
]

[tool.uv.sources]
ao-dev = { path = "../../..", editable = true }
