# Doc Bench

Document QA. Data quality is pretty low, I think we shouldn't use this benchmark.

## Clone

You need access to [the github repo](https://github.com/agops-project/Anni-Zou-DocBench).

```
git submodule update --init example_workflows/ours_doc_bench/doc_bench
```
