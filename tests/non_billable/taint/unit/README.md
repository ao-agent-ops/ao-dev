# Taint Unit Tests

These tests are used to test the taint wrappers in the `runner.taint_wrappers` module.

To run the tests, use the following command:

## NOTE: These tests were created by Claude Code. Use with caution.