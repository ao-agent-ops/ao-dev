# stubs/openai/resources/responses/responses.py

from typing import Any
from pyre_extensions import __test_source, __test_sink

class Responses:
    def create(self, model: str, input: Any, **kwargs: Any) -> Any:
        # mark the argument as a sink
        __test_sink(input)
        # propagate it back as a source
        return __test_source(input)
